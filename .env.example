# PostgreSQL destination
export PGHOST=localhost
export PGPORT=5432
export PGDATABASE=pianoweb
export PGUSER=testuser
export PGPASSWORD=change-me

# SQL Server source after restoring the .bak backup
# export MSSQL_CONNECTION_STRING='DRIVER={ODBC Driver 18 for SQL Server};SERVER=localhost;DATABASE=pianoweb_source;UID=...;PWD=...;TrustServerCertificate=yes'

